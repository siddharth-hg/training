<Window x:Class="HarbingerWatermarkOverlay.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Focusable="False">
    <!-- Entire overlay is non-interactive so clicks pass through -->
    <Grid IsHitTestVisible="False" x:Name="Root">
        <!-- Viewbox keeps the watermark + text scaling together -->
        <Viewbox x:Name="Scaler"
                 Stretch="Uniform"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center">
            <!-- Stack image and text on top of each other -->
            <Grid x:Name="WatermarkVisual" RenderOptions.BitmapScalingMode="HighQuality">
                <Image x:Name="Img" />
                <!-- Device name text -->
                <Border Background="#10FFFFFF"   
                        CornerRadius="8"
                        Padding="10"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                    <TextBlock x:Name="DeviceText"
                               Text="MACHINE"
                               Foreground="#D9FFFFFF"       
                               FontWeight="SemiBold"
                               TextAlignment="Center"
                               TextWrapping="Wrap"
                               FontFamily="Segoe UI"
                               FontSize="64"               
                               Opacity="0.85">
                        <!-- Soft outline/blur for contrast -->
                        <TextBlock.Effect>
                            <DropShadowEffect ShadowDepth="0" BlurRadius="6" Opacity="0.6" />
                        </TextBlock.Effect>
                    </TextBlock>
                </Border>
            </Grid>
        </Viewbox>
    </Grid>
</Window>